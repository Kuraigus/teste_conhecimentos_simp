const BookRepository = require("./book.repository"),
  UserRepository = require("./user.repository"),
  CloudinaryRepository = require("./cloudinary.repository");

module.exports = {
  BookRepository,
  UserRepository,
  CloudinaryRepository,
};
